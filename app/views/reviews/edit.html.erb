#create page/form, have two things to edit: rating + description, save.
header, within header, have edit review for the name of class you've taken.
then save the review, and bring me back to the class.

<%= render 'form' %>
