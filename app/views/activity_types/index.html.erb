<section class="clean-block clean-catalog dark">
<div class="container">
  <div class="block-heading">
    <h2 class="text-info">Available classes</h2>
  </div>

  <div class="content">
    <div class="products pl-5 card-grid-3c">
      <% @scheduled_activities.each do |scheduled_activity| %>
          <%= render partial: 'activity_card', locals: {scheduled_activity: scheduled_activity} %>
      <% end %>
    </div>
  </div>
</div>
</section>
